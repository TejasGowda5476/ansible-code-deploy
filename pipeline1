pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Run Ansible') {
            steps {
                script {
                    sh '/var/lib/jenkins/workspace/${JOB_NAME}/ansible-playbook deploy.yml'
                }
            }
        }
    }
}
